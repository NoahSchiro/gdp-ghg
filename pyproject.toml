[project]
name = "gdp-ghg"
version = "0.1.0"
description = "How does GDP of a country relate to its GHG emissions?"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.0",
    "pandas>=3.0.3",
]
